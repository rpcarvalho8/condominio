# Arquivo — pacotes fora do foco actual

- `mobile/` — Expo (legado do template)
- `desktop/` — Electron (legado do template)

A app activa é só `packages/web`. Estes pacotes foram movidos para aqui em Agosto 2026 para simplificar o monorepo; não entram no `bun run dev`.
